#include "main.h"

/**
 * _strspn - Get the length of a prefix substring that consists of only
 * characters in 'accept'
 * @s: The string to be searched.
 * @accept: The string containing characters to match against.
 *
 * Return: The length of the prefix substring in 's'
 * that consists only of characters present in 'accept'.
 * The length is calculated in bytes.
 */

unsigned int _strspn(char *s, char *accept)
{
}

#include "main.h"

/**
 * _memset - Function that sets a memory with a specified character
 * @s: The memory to set
 * @b: The character to set the memory with
 * @n: The number of bytes to set
 *
 * Return: The new memory
 */

char *_memset(char *s, char b, unsigned int n)
{
}

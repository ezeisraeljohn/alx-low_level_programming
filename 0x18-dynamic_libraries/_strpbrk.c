#include "main.h"

/**
 * _strpbrk - Locate the first occurrence in the string 's' of any character in
 * the string 'accept'
 * @s: The string to be searched.
 * @accept: The string containing characters to search for.
 *
 * Return: If a matching character is found, returns a pointer to the first
 * occurrence of that character in 's'. If no matching character is found,
 * returns NULL.
 */

char *_strpbrk(char *s, char *accept)
{
}

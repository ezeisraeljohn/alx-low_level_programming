#include <stdio.h>
#include <string.h>

/**
 * main - Convert all letters to lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c = 'a';
	while (c <= 'z')
		putchar(c++);
	putchar('\n');

	return (0);
}

	

